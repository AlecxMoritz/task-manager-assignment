import Layout from '../src/components/MyLayout';

const Manage = (props) => {

    return (
        <Layout>
            <h1>Manage Your tasks here!</h1>
        </Layout>
    );
};

export default Manage;